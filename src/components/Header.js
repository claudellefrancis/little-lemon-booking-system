import Nav from './Nav';
import logo from '../images/logo.png';

function Header() {
  return (
    <header>
      {logo}
        <div>
          Nav />
        </div>
    </header>
  );
}

export default Header;